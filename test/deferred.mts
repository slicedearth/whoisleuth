// Explicitly controlled asynchronous work for fixture-only lifecycle tests.
export function deferred<T>() {
  let resolve!: (value: T) => void;
  const promise = new Promise<T>((settle) => { resolve = settle; });
  return { promise, resolve };
}
