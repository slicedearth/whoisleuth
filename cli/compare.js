'use strict';

// Stable CommonJS entry point retained while the CLI runner and evidence
// export boundary remain JavaScript during the native TypeScript migration.
module.exports = require('./compare.mts');
