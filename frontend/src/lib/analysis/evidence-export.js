// Compatibility re-export for the Lookup workspace. The pure evidence builder
// is shared with the local CLI so both surfaces emit the same report contract.
export * from '../../../../lib/evidence-export.mjs';
