// Compatibility re-export for existing frontend consumers. The pure model is
// shared with the local CLI so both surfaces apply identical source semantics.
export * from '../../../../lib/registry-comparison.mjs';
