<script lang="ts">
  import PageHeading from '$lib/components/PageHeading.svelte';
  import PublicSeo from '$lib/components/PublicSeo.svelte';
</script>

<PublicSeo
  title="Privacy policy | WHOISleuth"
  description="How WHOISleuth processes registry data, browser-local investigation state, hosted monitoring and session information."
  path="/privacy"
/>

<PageHeading eyebrow="Policy" title="Privacy policy" description="How this deployment processes registry data, local investigation state, and session information." />

<div class="policy-layout">
<nav class="policy-index card" aria-label="Privacy policy sections">
  <p class="eyebrow">On this page</p>
  <a href="#privacy-introduction">Introduction</a>
  <a href="#privacy-information">Information processed</a>
  <a href="#privacy-use">How information is used</a>
  <a href="#privacy-legal">Legal basis</a>
  <a href="#privacy-third-parties">Third parties</a>
  <a href="#privacy-storage">Cookies and storage</a>
  <a href="#privacy-retention">Retention</a>
  <a href="#privacy-security">Security</a>
  <a href="#privacy-rights">Your rights</a>
  <a href="#privacy-transfers">International transfers</a>
  <a href="#privacy-children">Children's privacy</a>
  <a href="#privacy-changes">Changes</a>
  <a href="#privacy-contact">Contact</a>
</nav>

<article class="policy card">
  <p><em>Last updated: 29 July 2026</em></p>

  <h2 id="privacy-introduction">1. Introduction</h2>
  <p>This policy explains what WHOISleuth processes when you use it, why, and what choices you have. It applies to this deployment. By using the service, you agree to the practices described here.</p>

  <h2 id="privacy-information">2. Information processed</h2>
  <p><strong>Registry data.</strong> Looking up a domain returns whatever registrant contact data its registry or sponsoring registrar publishes through RDAP or WHOIS. The service relays this already-public registry-ecosystem data at your request; most sources redact it by default.</p>
  <p><strong>Registry interpretation in deep Lookup.</strong> For a deep non-compact domain Lookup, the server derives a bounded versioned interpretation from the RDAP, WHOIS, and optional registrar RDAP evidence already collected. It keeps lifecycle and lock statuses, contact-disclosure states, publication completeness, normalized source reconciliation, and published abuse-route contact points separately attributed. A complete response with no usable contact can be labelled absent, while partial, truncated, skipped, unsupported, or failed publication remains unavailable rather than becoming evidence of absence. The browser can organize usable registrar, registry, security.txt, and selected endpoint IP RDAP contacts already present in the completed response into a bounded route list. It performs no contact discovery or reachability check and saves a route into a browser-local case only after the analyst selects it. An IP RDAP contact belongs to the registered network of one observed endpoint and does not prove hosting responsibility or identify an origin server. Published contact routes can be displayed, recorded, and deliberately exported, but are not proof that a destination is monitored, appropriate, or responsible. A browser-derived acquisition workspace can organize those existing fields with point-in-time web and mail evidence into a manual checklist; it performs no valuation or additional request and does not establish eligibility, price, release timing, ownership, or acquisition success. This interpretation adds no request, does not affect availability or Risk, and is excluded from fast and compact Bulk paths.</p>
  <p><strong>Acquisition review export.</strong> The analyst may deliberately download a bounded integrity-stamped JSON review containing the normalized acquisition workspace, decision state, short rationale, completed and outstanding manual checks, observation and generation times, and explicit limitations. The artifact is generated locally, makes no request or submission, and excludes raw registry responses and expanded contacts. A reviewed state records checklist completion only, not the accuracy of an external statement or a legal, financial, ownership, eligibility, availability, or transfer determination. The public Demo marks its equivalent artifact as synthetic.</p>

  <p><strong>Custom Discover dictionary.</strong> Optional pasted or imported terms are normalized and capped at 100 unique entries, 32 characters per term, and 4,096 input characters. They remain transient in the current browser tab, are not sent to the server or saved in the browser-local workspace, and are used only after you select <strong>Generate candidates</strong>. If selected candidates continue to Bulk, only the resulting domain names and bounded mutation provenance follow the normal lookup path; the original dictionary list does not.</p>
  <p><strong>Registrar RDAP in deep Lookup.</strong> When a registry RDAP object publishes a complete HTTPS link for the same domain at the sponsoring registrar, a deep non-compact Lookup can relay that one public registrar object as a separately attributed source. It is briefly cached in server memory like other registry responses, displayed in the transient Lookup result, and never consulted by availability or scoring. Fast and compact Bulk requests do not perform the follow-up, and registrar RDAP is not copied into browser-local watchlists, cases, or other compact stores. The deliberate raw unified-response view can contain it. The structured Lookup evidence export can retain the normalized portable-field comparison between registry and registrar publications, including both displayed source values and source-health states, but excludes the registrar raw object, contacts, entities, links, notices, and source-specific handles.</p>
  <p><strong>Observed network context in deep Lookup.</strong> After the existing TLS and DNS collection, a deep non-compact domain Lookup can select one retained public endpoint address and perform one logical IP RDAP enrichment through the existing bounded safe-fetch and cache boundary. The transient result keeps a separately attributed network name and holder, handle, bounded CIDRs, address range, country, network type, database freshness, source provenance, and up to six published abuse-role email or phone routes. Raw IP RDAP data and other contact roles are excluded from this summary. Routes are excluded from the ordinary readable and evidence exports. A selected contact can be copied into a browser-local case action only after analyst review and can then appear in a deliberately generated response packet. The result is not copied into compact Bulk responses, watchlists, profiles, or monitoring state and never affects availability or Risk. The underlying public IP RDAP response is briefly cached in server memory like other RDAP responses. The selected address can belong to a CDN, proxy, load balancer, or shared edge and is not proof of an origin host, hosting control, ownership, intent, or maliciousness.</p>
  <p><strong>Deep DNS intelligence.</strong> The server performs bounded public A, AAAA, CNAME, NS, MX, SPF, DMARC, and CAA queries for a registered domain. Only SPF and DMARC policy TXT records are retained; unrelated TXT records are discarded. Deep non-compact domain Lookup can add one SOA query and retain its bounded zone-maintenance fields. It can also send one HTTPS resource-record query through up to three validated literal addresses from the deployment's configured DNS resolvers. The result retains bounded service priority, mode, effective target, TTL, ALPN identifiers, port, address hints, mandatory keys, and the names and lengths of other recognized parameters. Opaque parameter values are discarded. The same full Deep path can resolve effective CAA by reusing the exact-hostname answer and walking toward, but not including, the DNS root until the first non-empty RRset is found. The walk queries at most eight owner names, stops on resolver error, and keeps the direct and inherited policy separately attributed. Recursive alias processing remains the deployment resolver's responsibility. Fast, compact, Bulk, monitoring, availability and Risk paths do not run this parent walk. WHOISleuth does not follow service-binding aliases or connect to published targets, ports, or address hints, and it does not disclose the query to a new third-party DNS service. A deep non-compact Lookup of a public IP address can add one PTR query and retain up to eight normalized reverse-DNS names as a separately attributed source. Fast, compact, Bulk, monitoring, private or special-purpose IP, and availability or Risk paths do not run the PTR query. These requests use the deployment's DNS resolver. PTR, SOA, effective CAA, and HTTPS service-binding publications are public point-in-time context and do not prove ownership, hosting control, intent, or safety.</p>
  <p><strong>Analyst-controlled external evidence pivots.</strong> A completed Lookup can show a collapsed set of ordinary links to reviewed public registration, top-level-domain delegation, Certificate Transparency, archived-page, routing, interconnection, and site-status tools. The links are constructed only from a locally revalidated canonical domain, top-level domain, public address or prefix, or public ASN already present in the Lookup result. WHOISleuth does not prefetch those destinations, call their APIs, embed their pages, retain their responses, or use them for availability, Risk, cases, exports, Bulk, or monitoring. Each link names the destination and exact value it will receive. That value leaves WHOISleuth only after the analyst opens the link, under the destination's own privacy and retention terms.</p>
  <p><strong>Optional security.txt disclosure lookup.</strong> When selected for a deep single-domain Lookup, the server starts one bounded HTTPS collection at the standardized security.txt location on the exact entered hostname. It retains only normalized published contacts, policy and encryption references, preferred languages, canonical locations, expiry, and source health. The response body is discarded after parsing and no server cache is used. The normalized result can be displayed and deliberately exported, but it is not copied into compact Bulk responses, cases, watchlists, profiles, or monitoring state and never affects availability or Risk. Publication does not authorize testing or prove that a contact is monitored.</p>
  <p><strong>Optional archived-verdict search.</strong> If the operator explicitly enables the URLscan adapter and you select it for a deep single-domain Lookup, the server sends only the canonical registrable domain to URLscan's Search API. It searches existing public scan history and never submits the domain or URL for scanning. The provider also receives ordinary API request metadata and associates the query with the operator's API credential under its own privacy and retention policy. WHOISleuth keeps no provider cache; the bounded normalized response is displayed transiently, excluded from browser-local stores and the structured Lookup evidence export, and never affects availability. Fast and compact Bulk paths never make this request.</p>
  <p><strong>Optional malware-host search.</strong> If the operator explicitly enables the URLhaus adapter and you select it for a deep single-domain Lookup, the server posts only the canonical registrable domain to URLhaus's host API. It searches existing malware-distribution records and never submits a URL, sample, or report. The provider also receives ordinary API request metadata and associates the query with the operator's API credential under its own privacy and retention policy. WHOISleuth keeps no provider cache; the bounded normalized response is displayed transiently, excluded from browser-local stores and the structured Lookup evidence export, and never affects availability. Fast and compact Bulk paths never make this request. Community access is subject to not-for-profit fair-use terms; commercial deployments may require a paid provider agreement.</p>
  <p><strong>Optional malware-IOC search.</strong> If the operator explicitly enables the ThreatFox adapter and you select it for a deep single-domain Lookup, the server sends only the canonical registrable domain in an exact-match search to ThreatFox. It searches retained malware indicators and never submits an IOC, URL, sample, or report. The provider also receives ordinary API request metadata and associates the query with the operator's abuse.ch credential under its own privacy and retention policy. WHOISleuth keeps no provider cache; the bounded normalized response is displayed transiently, excluded from browser-local stores and the structured Lookup evidence export, and never affects availability. Fast and compact Bulk paths never make this request. Older indicators expire from the community API, and commercial deployments may require a paid provider agreement.</p>
  <p><strong>Derived external Risk context.</strong> Optional provider payloads stay transient and separately attributed. A lone publisher, neutral miss, failed provider, unknown provider, or non-phishing/non-malware category adds no Risk points. When positive qualifying records are corroborated across at least two independent publisher families, Risk model v6 can add one bounded factor. Multiple datasets operated by the same publisher count as one source. Browser-local cases and reports can retain the resulting score, model version, and factor label, but not raw provider findings, references, or payloads.</p>
  <p><strong>Technical data.</strong> Standard request metadata, including IP address and timestamps, is processed transiently for login and rate limiting. When the operator enables optional distributed operation limits, the configured counter service stores only operation classes, opaque lease identifiers, expiry timestamps, and a one-way hash of the already-opaque session fingerprint. If durable usage accounting is also enabled, it stores bounded operation-feature identifiers, fixed 24-hour/30-day bucket identifiers, and integer counts. It receives no lookup targets, evidence, responses, browser-local records, or session tokens; leases expire after five minutes and usage counters expire shortly after their fixed window ends.</p>
  <p><strong>Optional hosted scheduled monitoring.</strong> This is disabled by default. When the operator explicitly enables the Netlify worker and a scheduled watchlist is present, it retains the bounded watchlist name, canonical domains, interval, timestamps, compact fast registration evidence, six recent change events, and an opaque resumable run cursor. It never stores raw RDAP or WHOIS payloads, expanded contacts, analyst notes, browser sessions, or deep website content. The complete state is encrypted and authenticated with AES-256-GCM before it is written to the site-wide Netlify Blob store; Netlify stores the ciphertext and ordinary object metadata, while its function runtime necessarily processes the decrypted state transiently to run requested public lookups. The scheduled worker has no public route. A separate authenticated management route lets a signed-in user deliberately schedule a browser-local watchlist, read the bounded hosted projection, pause or resume it, replace its hosted snapshot, restore that compact snapshot into the current browser, or delete the hosted copy. Mutations require a same-origin request and request bodies are capped at 1 MiB. This deployment uses one shared login and has no per-user roles or audit identities, so every person given that login can view and manage the same hosted scheduled-watchlist state. Restoring a snapshot creates or replaces a browser-local watchlist only after explicit confirmation. Disabling the worker stops Blob and lookup work but does not delete existing ciphertext; the operator or an authenticated user must remove hosted state deliberately when its history is no longer required. Replacing or losing the encryption key without migrating the state makes the retained ciphertext unreadable.</p>
  <p><strong>Deep lookup evidence.</strong> A requested deep lookup can display bounded HTTP metadata and static page-identity fields derived from the homepage response already fetched for analysis. Page identity can include language, canonical and meta-refresh targets, selected Open Graph fields, generator metadata, forms, normalized resource counts, external resource and embedded origins, mail-contact domains, download context, recognized public tracking identifiers, and versioned page fingerprints. Fingerprints cover the exact captured body, noise-reduced normalized HTML, visible text, static tag structure, form structure, external resource hosts, and public tracking identifiers. Intermediate normalized markup and visible text are discarded after hashing. URL credentials, queries, fragments, resource and download paths, form-action paths, and complete email addresses are not retained. Deep Lookup can also examine JSON-LD already present in that captured response and retain only curated schema types, bounded labels, declared HTTP(S) origins, and normalized sameAs hostnames. Raw JSON-LD, contact fields, arbitrary properties, complete URLs, paths, queries, and fragments are discarded, and referenced JSON-LD is not fetched. Publisher-declared metadata does not prove identity, ownership, control, safety, or maliciousness. Deep Lookup can classify capped live input elements already present in that response from semantic type and autocomplete declarations. It retains only fixed counts for password, email, username, one-time-code, and payment-related purposes, form methods, and same-origin, external-origin, missing, cleartext, or unclassified action relationships. Category counts can overlap, and cleartext is a transport subset of a same-origin or external-origin count. Field names, values, labels, placeholders, arbitrary attributes, complete action URLs, paths, queries, and fragments are never retained. External form submission can be legitimate and is not a phishing, vulnerability, ownership, intent, or maliciousness finding. This analysis makes no additional request, does not interact with a form, does not affect availability or Risk scoring, and is excluded from compact Bulk results and browser-local cases, watchlists, and profiles. It can appear in a deliberate full Lookup evidence export. Deep Lookup can also derive a versioned technology profile from the selected HTTP server header, generator metadata, normalized resource origins, and capped static HTML already collected. It retains only curated technology names, categories, confidence levels, evidence classes, and fixed explanations. A nested browser-library profile can also match up to 64 observed script elements and 65,536 cumulative inline-script characters against a pinned local Retire.js catalogue. It retains only the apparent component and version, detection method, bounded advisory identifiers, weakness classes, severity, catalogue version, source health, and fixed limitations. Referenced scripts are not fetched or executed, and script references, paths, queries, matched inline content, and hashes are not retained. An advisory match does not establish reachability or exploitability. The technology profile does not retain matched markup, arbitrary header values, paths, or signature input, and an unmatched signature is not evidence that a technology or browser library is absent. Deep Lookup can also derive fixed page-role labels and static client-side behaviour indicators from the same capped parse. Referenced scripts are not fetched or executed, matched page and script source is not retained, and these heuristics do not prove purpose, legitimacy, tracking, vulnerability, credential theft, or maliciousness. Deep Lookup can separately interpret the existing HTTP response, bounded static form and resource summaries, one TLS handshake, DNSSEC publication, and CAA query as a versioned passive security-posture profile. That profile retains fixed finding identifiers, categories, state and tone labels, explanations, evidence classes, and bounded counts rather than header values, TLS errors, URLs, certificate contents, DNS record contents, or page markup. Observed absence is limited to the selected response or retained static evidence. These analyses make no additional request, perform no active vulnerability testing, and do not affect availability or Risk scoring. The complete rich page-identity, credential-surface, structured-identity, technology, role, behaviour, and passive-posture records are excluded from compact browser-local cases, watchlists, profiles, and Bulk results, but they can appear when the user deliberately downloads the full Lookup evidence response. When the active Brand Profile has a compatible official-site baseline, Lookup compares these bounded components locally and independently. A transient brand-mimicry review can organize those components with existing favicon, official-asset, password-field, and bounded review-language observations. It creates no combined page-similarity, mimicry, or maliciousness score, does not change the Risk score, and does not persist or export the derived comparison. Deep Lookup can also organize already collected DNS aliases, nameservers, web routing, mail publication, TLS source health, lifecycle statuses, and transfer locks into transient manual dependency, transition, and registry-policy review prompts. A fixed local catalogue can label recognized alias service families, and analysts can enter reviewed expected targets or namespaces for a transient in-scope or outside-scope comparison. The entered scope is not retained or sent to the server. Alias targets are not followed, provider accounts are not queried, claimability is not tested, and a catalogue or scope match never establishes configuration, ownership, service assignment, abandonment, or vulnerability. Monitor can derive a capped local graph and table from exact nameserver-set and final-origin observations already retained in browser-local case histories. Analyst-retained Bulk relationship records pass through a bounded disposable typed envelope before the local search and graph projection uses them. That envelope makes no request, performs no write, creates no additional stored copy, and leaves the IndexedDB record authoritative. A deliberate local graph download can include filtered case domains, exact relationship values, methods, classifications, sources, observation times, completeness, truncation, limitations, and up to 8 bounded source observations per relationship as versioned JSON, GraphML, or GEXF. It excludes case notes, status, disposition, raw registry or page responses, contacts, credentials, and transient graph view state. These comparisons and downloads make no additional requests. Fingerprints and shared observations support comparison but do not prove authorship, ownership, coordination, intent, or maliciousness.</p>
  <p><strong>Browser-local relationship context.</strong> Monitor can show how frequently a retained relationship appears among comparable cases in the current browser workspace. This local calculation is not sent anywhere, does not estimate wider internet prevalence, and does not strengthen attribution. A filterable local lifecycle view can also organize saved action due and follow-up dates with domain-expiry evidence and analyst-pinned certificate or disclosure review dates. It links each event to its local case while excluding recipient values, notes, pinned values, and source payloads.</p>
  <p><strong>Authoritative DNS health.</strong> Deep non-compact domain Lookup can compare the recursive nameserver view with the registry RDAP nameserver and glue publication, then send direct NS and SOA queries to at most four selected nameservers and two validated public addresses per nameserver. Registry, recursive, and direct evidence remain separately attributed. Private and reserved addresses are rejected before a direct query. A refused, unreachable, truncated, or otherwise incomplete observation remains explicit and is not treated as record absence. The registry DNSSEC review compares bounded publication fields only and does not validate the cryptographic chain. This collection is excluded from Fast and compact Bulk lookups and never decides registration availability, ownership, control, intent, safety, or maliciousness.</p>
  <p><strong>Source-specific Lookup refresh.</strong> Lookup can offer a deliberate retry for a limited RDAP, WHOIS, or grouped domain-evidence source, and can offer the same source groups after the displayed unified observation becomes stale. Each retry repeats only the stated bounded endpoint and displays a new source result with its own observation and supersession time. Repeated refreshes form a capped versioned chain in page memory. That chain is cleared with the page and does not merge into, replace, save, or export the original unified response. A limited or failed retry remains inconclusive and is not evidence of absence or safety.</p>
  <p><strong>DNS change rehearsal.</strong> After reviewing authoritative DNS health, an analyst can enter complete intended nameserver, glue, DS, MX, CAA, and critical address sets, a DNSSEC change type, and readiness confirmations. The browser keeps those assertions separate from observed records and derives an ordered procedural and rollback checklist locally. The entries are not saved automatically or sent to the server. A deliberate checklist download contains the domain, normalized observed and proposed sets, findings, unknowns, sequence, rollback steps, and limitations. The rehearsal makes no request, changes no DNS or registry state, verifies no provider account or authorization, and cannot guarantee propagation, correctness, availability, or a successful change.</p>
  <p><strong>Response-policy analysis.</strong> Deep Lookup can transiently review bounded Content-Security-Policy, Strict-Transport-Security, Referrer-Policy, and response-cookie attributes from the selected homepage response without making another request. It retains only fixed finding identifiers, bounded cookie counts, component health, and limitations. Complete header values, cookie names and values, paths, domains, nonces, hashes, and reporting endpoints are discarded. A finding is limited to that response, is not a vulnerability or exploitability verdict, and does not affect availability or Risk.</p>
  <p><strong>Technology-origin calibration.</strong> A site-builder or commerce platform is not identified from a retained third-party resource origin alone; that origin must be corroborated by page, generator, passive allowlisted response-header, or additional storefront evidence. The server can transiently compare only the fixed header names used by the curated catalogue, including selected platform, runtime, framework, and delivery trace headers. It retains the fixed finding and evidence class rather than the header name's arbitrary value. Delivery origins remain separately attributed. The offline signature benchmark uses synthetic fixtures and does not process user lookups.</p>
  <p><strong>Certificate policy context.</strong> Deep Lookup can locally compare the separately attributed effective CAA publication with the observed leaf-certificate issuer through a fixed conservative issuer mapping. It can also compare the observed issuer, SAN names, or public-key digest with an analyst-reviewed Brand Profile baseline for that official domain. Aside from the bounded inherited-CAA walk described above, this comparison makes no new request. Unknown issuer mappings and incomplete DNS or TLS remain indeterminate. Current CAA cannot establish the policy that applied when an existing certificate was issued, and a difference does not establish improper issuance, compromise, ownership, intent, safety, or maliciousness and does not change Risk.</p>
  <p><strong>TLS and certificate intelligence.</strong> A requested deep domain scan opens one direct TLS connection to one validated public address while retaining the domain as SNI. Lookup and its deliberate evidence export can include the connected address, negotiated protocol, cipher and ALPN, runtime trust and hostname outcomes, bounded certificate identity, validity, SAN and public-key metadata, signature algorithm and OID, extended-key-usage purposes, fixed SAN class counts, classified Authority Information Access presence counts, and a capped chain summary. Email, URI, directory-name, registered-ID, other-name, and unclassified SAN values are counted but not retained. AIA responder and issuer locations are classified as HTTP, HTTPS, or other, then discarded without being followed. Certificate bytes and TLS session material are not retained. Eligible deep non-compact domain Lookup compares the observed leaf certificate's SHA-1 provider identifier with a generated local SSLBL snapshot. That comparison makes no runtime request to SSLBL and retains only the exact fingerprint, snapshot date and digest, source state, verdict, and limitations in the transient result and deliberate evidence export. A miss, stale snapshot, or unavailable snapshot is not evidence of safety or absence, and the result does not affect availability or Risk. The richer profile is excluded from compact browser-local cases, watchlists, profiles, and Certificate Transparency history. Compact Deep Bulk can retain only one bounded issuer label, one exact SPKI SHA-256 fingerprint, and the TLS source state for reviewed comparison; it does not retain the complete certificate profile.</p>
  <p><strong>Analyst-selected relationship observations.</strong> Bulk can compare bounded nameserver, IP-address, native certificate, public tracking-identifier, favicon, and configured official-asset-host observations inside the current result set. Nothing is saved automatically. Selecting <strong>Retain observation</strong> stores only one normalized relationship value, its bounded member domains, method, observed and retained times, source and schema version, completeness, truncation, fixed description, and stated limitations in the browser's IndexedDB database. It excludes the complete Bulk result, raw RDAP, WHOIS or page responses, contacts, credentials, and unrelated DNS or HTTP evidence. Monitor can review or delete the record, local search can find its known fields, and the typed projection can connect it to existing cases without a new request. The record is derived evidence and does not prove ownership, coordination, intent, or maliciousness.</p>
  <p><strong>Audience measurement.</strong> The service does not use advertising, behavioural profiling, or cross-site tracking. Any privacy-preserving audience measurement introduced by the operator must stay on public pages and be described in this policy before it is enabled. Protected investigation routes, lookup terms, saved evidence, and session identifiers remain outside that scope.</p>

  <h2 id="privacy-use">3. How information is used</h2>
  <ul><li>To perform a lookup, scan, posture audit, or search you request.</li><li>To keep you signed in.</li><li>To enforce limits that protect the service and upstream registries.</li></ul>
  <p>Published registrant data is not used to build profiles, sold, or shared with advertisers.</p>

  <h2 id="privacy-legal">4. Legal basis</h2>
  <p>Where GDPR or similar law applies, operation of requested lookups and anti-abuse controls relies on legitimate interest. Exporting, saving, or acting on displayed data is your decision. Any registrant outreach should remain low-volume, human-reviewed, compliant with applicable law, and stop when requested.</p>

  <h2 id="privacy-third-parties">5. Third parties</h2>
  <p>Requests may reach the relevant registry or registrar, public DNS, a domain's homepage, favicon, TLS endpoint, optional security.txt endpoint, or MTA-STS policy host, <a href="https://crt.sh/" target="_blank" rel="noopener">crt.sh</a>, and the infrastructure hosting this deployment. The local SSLBL comparison makes no provider request. Opening its separately labelled matching-record link is deliberate browser navigation to SSLBL and discloses the matched certificate fingerprint under that provider's ordinary privacy and request-handling terms. When the optional archived-verdict adapter is configured and explicitly selected, <a href="https://urlscan.io/privacy/" target="_blank" rel="noopener">URLscan</a> receives the canonical registrable domain and ordinary API request metadata for a search-only request under its own terms, privacy policy, and account quota. When an optional abuse.ch adapter is configured and explicitly selected, <a href="https://abuse.ch/privacy-policy/" target="_blank" rel="noopener">URLhaus or ThreatFox</a> receives the same bounded domain target and ordinary request metadata for a host or exact-match IOC lookup under its own fair-use terms, privacy policy, account quota, and retention rules. These integrations do not submit a target for scanning or reporting. If distributed operation limits are enabled, their configured REST service processes the minimal lease and optional fixed-window counter metadata described above. If hosted scheduled monitoring is enabled, Netlify Functions performs the bounded lookups and Netlify Blobs retains its application-encrypted state and ordinary object metadata. No advertising or CRM processors are used. Any audience-measurement processor would be identified here before it is enabled.</p>

  <h2 id="privacy-storage">6. Cookies and browser storage</h2>
  <p>The service sets one signed session cookie, <code>wrt_session</code>, for up to 30 days. It is HttpOnly, SameSite=Lax, and Secure over HTTPS. It is required for authentication and is not used for tracking.</p>
  <p><strong>Appearance preference.</strong> The Theme selector can retain one bounded <code>dark</code>, <code>light</code>, or <code>system</code> value in this browser's local storage under <code>whoisleuth:theme:v1</code>. It is not sent to the server. It is included only when you deliberately download a unified workspace archive so the receiving browser can restore the selected appearance. Without a saved value WHOISleuth follows the browser's operating-system preference. Clearing site data removes the preference.</p>
  <p>Brand profiles, shortlist entries, ordinary watchlists, analyst cases, campaigns, custom rules, retained relationship observations, investigation templates, and bounded Certificate Transparency search baselines stay as bounded records in your browser's IndexedDB database. They are not sent to the server and are visible to whoever can use that browser profile. On the first authenticated load after the storage change, WHOISleuth normalises supported legacy local-storage documents and copies them into a versioned IndexedDB manifest without deleting the source documents. Later IndexedDB writes are authoritative and do not automatically update those legacy sources. A deliberate Dashboard control can refresh the legacy compatibility copies before returning to an older build, subject to local-storage quota. The IndexedDB record codec is currently plaintext JSON; SHA-256 manifest digests detect corruption but are not encryption. A downloaded workspace archive remains the safer portable backup and can be wrapped in passphrase-based authenticated encryption entirely in the browser. This protects the downloaded file while locked, not the active IndexedDB database or an open Console. Only a separately selected scheduled watchlist may enter the optional encrypted hosted store described above. Campaigns contain a bounded label, optional description, and normalised case-domain membership only; they do not copy case evidence, notes, status, or disposition. An expanded campaign transiently projects fixed cue counts, unavailable membership, limited evidence, and unreviewed dispositions from each linked case's latest retained snapshot without another request or stored copy. The projection is not included in campaign export and is not a score, ownership claim, attribution finding, or maliciousness determination. Cases can additionally retain bounded analyst-selected evidence pins, field-level checkpoints, decision rationales, deliberately selected contact routes, reviewed response actions, follow-up dates, references, outcomes, and fixed evidence-gap dismissal outcomes. A checkpoint copies only selected normalized facts and their source, observation time, collection depth, source state, completeness, truncation, schema version, and limitations; it excludes raw registration payloads, expanded contacts, HTML, scripts, provider payloads, and unselected fields. An evidence-gap dismissal records the reviewed reason and exact current gap fingerprint in the case investigation trail without resolving, deleting, or rewriting any underlying pin or assertion. These analyst-authored records stay separate from collected evidence snapshots and can contain sensitive investigation context. Monitor can transiently project the saved contact sources and review dates into a local route-review list without discovering or checking any contact. A deliberate iCalendar download contains bounded domain, case identifier, action type, state, and review dates but excludes recipient values, notes, evidence values, and source payloads. Certificate and security.txt expiry reviews are included only when the analyst explicitly pins those facts to a case. A selected security.txt result can also be organized transiently into current, expiring, expired, partial, or unavailable disclosure health using only its normalized fields; this adds no request or reachability check. Brand Profiles can retain six bounded analyst-owned protection attestations, their review states, optional expiry dates, and short notes for registrar MFA, recovery-email separation, registry lock, emergency contacts, account audit logging, and zone backups. These attestations are not inferred from public evidence and are included only in deliberate profile or workspace exports. A Brand Profile can also retain reviewed desired nameserver, DS, MX, CAA, TLS issuer, SAN patterns or public-key digest, transfer-lock, and renewal-review values; reasoned suppressions; and one explicitly retained compact posture observation per official domain. These analyst-authored values are not sent with a posture request, do not change infrastructure, and appear only in deliberate profile, baseline, or workspace exports. Unsupported or incomplete public evidence never becomes an aligned result. The posture UI transiently groups the selected profile and returned checks into a desired-state review and dependency review without another request or stored copy. Lookup can also transiently compare already-observed CNAME, HTTPS alias, nameserver, mail-server, and final HTTP-origin targets with a bounded service-family catalogue, analyst-entered authorized scope, and exact false-positive targets. Catalogue matches can display the local evidence class, source and licence treatment, source date, review age, and catalogue digest without making another request. Those two text inputs remain local to the current view and are not retained; the resulting candidate, unresolved, active, unsupported, or false-positive state never establishes a dangling, claimable, abandoned, vulnerable, safe, malicious, owned, or controlled service. Watchlists and cases may retain compact HTTP facts from deep checks: the final origin without its path or query, response status, transport, redirect count and flags, MIME type, and which selected security headers were present. Header values are not retained. CT baselines retain normalised search keywords, observed public domains, timestamps, and result counts so later searches can identify new observations; they can be deleted individually or cleared from Discover. A posture audit sends only the selected official domain, its fixed standard, defensive-no-mail, or parked profile choice, and configured active and retired DKIM selector names to this deployment. The server then queries the public registry, DNS, literal bounded SPF dependencies, external DMARC reporting-authorization names, and the advertised official MTA-STS policy host. Supported DKIM public keys are parsed transiently for supported algorithm and size evidence and are not stored by the server. Clearing site data removes all saved browser-local state but does not delete separately retained hosted-monitoring ciphertext.</p>
  <p><strong>Saved Bulk sessions.</strong> A Bulk investigation is retained only after the analyst names and saves it. The bounded record contains the domain queue, scan mode, compact settled result fields, per-source completion states, and timestamps needed to load, compare, export, or resume unstarted domains. Compact Deep rows can retain one nullable null-MX observation alongside the existing MX, SPF, and DMARC booleans. Saved session schema 2 can also retain a versioned comparison envelope containing at most 12 normalized technology identifiers, one bounded TLS issuer label, one exact SPKI SHA-256 fingerprint, and independent technology and TLS source states. Schema 1 sessions remain readable and do not invent those fields. The envelope is derived from evidence already collected for the compact Deep row and makes no additional request. It excludes complete technology evidence, script references, certificate profiles, alternative names, chains, session material, and raw page or TLS payloads. The local lookalike mail-exposure review uses its bounded fields, source coverage, mutation provenance, registration state, and the active Brand Profile's configured mail posture. It makes no additional request and never connects to SMTP, sends a message, tests a mailbox or catch-all behavior, or retains message data. Its optional JSON export is generated locally, includes an integrity digest and stated limitations, and excludes raw DNS responses, contacts, scripts, and provider payloads. Saved sessions exclude raw source payloads, complete Lookup responses, registrant and abuse contacts, and Certificate Transparency rows. Saving and loading make no network request. An explicit resume sends only domains without a settled row through the selected Bulk mode.</p>
  <p><strong>Source-qualified case sightings.</strong> A case can retain a bounded sighting with a fixed state, evidence category, short source label, observation or review time, completeness, optional evidence-pin reference, and limitations. Deployment observations, provider reports, and analyst-reviewed states remain distinct. Recording not reproduced or expired as an analyst state does not remove, rewrite, or negate the original evidence. Sightings remain browser-local unless included in a deliberate case, workspace, or STIX 2.1 export. The STIX export runs locally, represents affirmative sightings as separately attributed observations, retains every state as a bounded note, and never publishes or transmits the bundle.</p>
  <p><strong>Website profile snapshots.</strong> A snapshot is retained only after the analyst explicitly saves a completed Deep Lookup. The bounded record contains the canonical domain, observation and save times, collection completeness and truncation, curated technology identifiers, passive posture states, selected page-identity digests, up to 30 normalized resource hosts and tracking identifiers, up to 20 external form-action origins, and source-health states. When the reviewed result contains a normalized leaf certificate, it can also retain SHA-256 and SPKI SHA-256 fingerprints, bounded subject and issuer labels, serial, validity dates, authorization and hostname-match states, and TLS completeness. It does not retain certificate bytes or the expanded alternative-name list, does not refresh saved observations, and labels them as deployment-observed browser history rather than current certificate state. It excludes form paths and queries, complete URLs, raw RDAP, WHOIS, HTTP, HTML, contact, credential, and provider payloads. Comparing, deleting, exporting, and importing snapshots happen locally and make no request. Monitor can build a disposable capped view that groups exact compatible observations across saved history and calculates explainable weighted relationships from the latest compatible snapshot for each domain. Every contributing field and weight remains visible, while unavailable or incompatible components add no weight. Search and grouping remain in browser memory and create no additional stored copy. A deliberate action can record a selected cluster as a separately typed analyst assertion in one browser-local case. Added, removed, changed, shared, similar, unavailable, or incomparable fields are review leads, not evidence of compromise, ownership, authorship, coordination, hosting, intent, safety, or maliciousness.</p>
  <p><strong>Retained-evidence timeline.</strong> Monitor can project bounded metadata from deliberately retained case evidence, evidence pins and checkpoints, external assertions, website-profile snapshots, saved Bulk sessions, watchlist checks, and relationship observations. The projection stays in browser memory, keeps observation time separate from storage time, and links to the owning browser-local record. Area and freshness filters use the retained observation time and stated bounded thresholds to identify older records; they do not refresh evidence or establish a domain's current state. The projection does not duplicate raw payloads, evidence values, relationship values, analyst notes, or page content, and does not start collection.</p>
  <p><strong>External intelligence import.</strong> A selected local STIX 2.1 or MISP JSON file is decoded, hashed with SHA-256, normalized, previewed, and merged entirely in the browser. Only bounded supported domain, hostname, URL, IP, ASN, or SHA-256 certificate-fingerprint claims and their provenance metadata can be retained on an explicitly selected existing case. Unsupported attribute values, raw files, descriptions, MISP comments, and provider payloads are not stored. Imported claims remain separate from collected evidence. The importer makes no network request, does not create a case, and does not start collection, scoring, publication, correlation, or reporting.</p>
  <p><strong>External finding conversion and sanitised capture import.</strong> The local case importer can convert a fixed-column CSV or JSON finding list, or documented domain, DNS, or certificate observation rows, into the strict WHOISleuth findings schema before preview. Conversion reports accepted, rejected, duplicate, and truncated counts without copying rejected values. It retains only a domain, evidence category, bounded summary, observation time, completeness, limitations, and optional bounded reference. Sanitised web-capture schemas can accept normalized domains and HTTP(S) origins, bounded titles, technology labels, request domains, and screenshot or DOM-digest metadata containing a plain file name, MIME type, declared size, dimensions where applicable, and SHA-256 digest. Artifact bytes are not imported or independently verified. Those schemas reject raw HTML, screenshots, archives, path traversal, cookies, request bodies, complete URLs, and arbitrary capture fields. Separate strict WARC and WACZ paths parse at most 8 MiB locally, discard request records, sensitive headers, downloads, compressed HTTP response bodies, non-HTML responses, invalid targets, excessive records or bodies, and mismatched supported record digests, and retain no raw archive content. WACZ packages are never replayed: ZIP paths, entry counts, extracted and expanded bytes, the data-package manifest, declared WARC resource sizes, and SHA-256 resource digests are validated before the same WARC privacy filter runs. The optional WACZ manifest digest is checked when present and its absence remains a limitation. Supported block digests and whole-file SHA-256 digests are calculated in the browser. Only normalized page findings reach the same deliberate preview and case import. All paths stay in browser memory until the analyst approves the ordinary separately attributed case import, make no network request, and do not independently refresh the imported observation.</p>
  <p><strong>Privacy-safe browser handoff.</strong> A Dashboard form can reduce a pasted domain or HTTP(S) URL to the normalized hostname or a sanitized HTTP(S) origin. URL credentials, port, path, query, fragment, and browser-local identifiers are discarded in browser memory. The default destination is a prefilled Deep Lookup and does not start collection. An analyst may instead type an exact loopback companion endpoint or HTTPS external endpoint; WHOISleuth does not discover, validate, poll, or retain that configuration. Before opening, the form shows the exact disclosed value, destination URL, and local, deployment, or third-party visibility and requires explicit confirmation for non-Lookup destinations. An optional separate action records only the prepared destination kind and normalized domain in a selected browser-local case trail; it never stores a returned result.</p>
  <p><strong>Reviewed static page-pattern packs.</strong> Monitor can install fixed, inspectable local detection-rule packs using only compact evidence already retained in browser-local cases. The included patterns combine separately observed password-field, off-origin form-action, favicon, official-asset, urgent-language, wallet-prompt, mail, and registration cues. Only whether an already observed static form action was external is copied into compact case evidence; its origin, path, query, fields, and values are excluded. Generic patterns remain labelled separately from brand-relative comparisons. Built-in and imported packs use the same bounded declarative validator, allowlisted fields and operators, and zero-score contract; they cannot execute code. They make no request, do not change built-in Risk, and produce review tags rather than phishing, ownership, intent, control, or maliciousness findings. An installed pack is an ordinary browser-local custom-rule copy that can be inspected, changed, exported, or deleted.</p>
  <p><strong>Investigation templates.</strong> A template must derive from one of the three standard guides. It can customise bounded labels and instructions, expected evidence and completion criteria, omit an allowlisted step, or add an approval gate. It cannot add arbitrary routes or operations, execute code, start collection, submit evidence, change a case, or remove a mandatory gate. Saving, editing, importing, exporting, and deleting templates happen locally and make no request. An optional restricted CACAO 2.0 export represents the same allowlisted workflow as a connected sequence of manual analyst steps. Import rejects executable or encoded commands, branches, targets, credentials, and unknown capabilities, and never executes command text.</p>
  <p><strong>Bulk filters, groups and selected actions.</strong> Filters and group summaries are calculated locally from compact rows already in memory. Explicit batch selection uses the bounded browser-local shortlist and does not make a request. A selected deep rescan sends only those domains; case, disposition, export and Monitor actions operate only on the analyst-controlled selection. Missing provider, ASN, hosting, registration or mail evidence stays unavailable rather than becoming a negative finding.</p>
  <p><strong>Public synthetic demo.</strong> The unauthenticated demo uses fixed fictional fixtures on reserved domains to represent Dashboard, Brands, Discover, Bulk, Lookup, and Monitor without performing a live analysis request. Its bounded stage flags, selected fixture identifier, synthetic case status/note, and follow-up state stay only in the current tab's <code>sessionStorage</code> under <code>whoisleuth:synthetic-demo:v1</code>, never enter production browser-local stores, and are removed by Reset demo or when the tab session ends. Downloaded demo packages are explicitly marked as synthetic and are not live findings or evidence reports.</p>
  <p><strong>Offline practice scenarios.</strong> The public Guide includes one fixed fictional decision exercise for each guided investigation type. Choices and feedback stay only in the component's current memory, make no request, write no browser storage, and disappear on reload. They are training examples, not findings or evidence reports.</p>
  <p><strong>Transient Console navigation state.</strong> Lookup and Bulk keep their current form values, results, filters, sorting, and pagination in memory during authenticated client-side navigation in the same tab. This includes the complete Lookup response currently displayed, with its separately attributed raw RDAP and WHOIS source sections, rather than a compact evidence snapshot. This state is not written to <code>localStorage</code>, <code>sessionStorage</code>, IndexedDB, the server, or a workspace archive. Signing out, a full reload, or closing the tab clears it. Deliberate case, watchlist, shortlist, download, and archive actions remain the only ways those tools retain or export selected evidence.</p>
  <p><strong>Evidence cluster and transition reviews.</strong> Monitor can build bounded connected clusters from retained relationship observations and qualify high-degree shared infrastructure. Labels, splits, merges, and dismissals change only the current review view; a deliberate JSON export includes the reviewed projection, exact contributing relationship values, case domains, provenance, completeness, and limitations without case notes, contacts, credentials, or raw upstream responses. A field checkpoint can separately retain one analyst-declared acquisition-transition expectation per selected fact: preserve, change, or manual review. A later Lookup keeps unavailable, conflicting, missing, and uncollected comparisons indeterminate. Neither feature establishes ownership, acquisition completion, service health, coordination, intent, or maliciousness.</p>
  <p><strong>Guided investigations.</strong> An authenticated user can optionally start a standard brand-sweep, infrastructure-pivot, or new-domain-triage guide for one canonical domain, or a bounded analyst-authored template derived from one of them. Schema version 4 keeps the recipe identifier, optional compact template snapshot, official or starting domain, optional analyst-selected candidate domain, up to 25 canonical domains carried from a guided Bulk comparison, an explicit truncation marker, creation and update times, active or paused state, bounded stage approval, opened, and outcome markers, and an optional review reason of up to 500 characters for partial or skipped steps in the current tab's <code>sessionStorage</code> under <code>whoisleuth:investigation-guide:v4</code>. Version 1, 2, and 3 records can normalise without inventing a template or review reason; future records remain untouched. Guide progress is not sent to the server or treated as evidence completion. The guide can pre-fill or preserve a bounded profile, discovery, lookup, Bulk, or case target and carries the bounded comparison set into a Monitor review queue without creating cases automatically. It focuses the relevant tool control and can show a compact return control while its main panel is outside the viewport; none of those behaviours submits the focused form. A network stage displays its request implications and requires an explicit approval marker before navigation, but opening that tool still never starts a lookup, search, scan, submission, export, or Monitor action. <strong>Export summary</strong> requires confirmation and deliberately downloads only a versioned compact progress record, including any bounded stage-review reasons, without raw evidence, case notes, credentials, provider responses, or scan results. A read-only local checkpoint and case-handoff checklist derive retained typed counts and workflow structure without deciding stage completion or making a claim about the target. <strong>End guide</strong> removes current and migrated legacy tab records, and closing the tab session removes them with the rest of that tab's session storage.</p>
  <p>CSV, JSON, Markdown, HTML, GraphML, GEXF, and other stated local exports are generated in your browser. Campaign exports contain their labels, descriptions, domain membership, and timestamps, so review them before sharing. A deliberate Risk calibration export includes only explicitly selected case IDs, domains, reviewed dispositions, and a bounded whitelist of normalized scoring inputs from the latest retained evidence. It excludes notes, tags, assertions, actions, contacts, raw source data, provider payloads, and stored Risk scores. The export is not anonymous, is not uploaded, and does not train, tune, or change the Risk model. Relationship graph exports contain the bounded filtered relationship evidence described above. Domain, IP, and ASN Markdown reports keep source states, collection time, normalized findings, and limitations from bounded known-field projections while excluding raw RDAP and WHOIS responses, expanded contacts, provider payloads, scripts, and remote assets. Domain reports additionally include the explainable Risk context; IP reports can include bounded reverse-DNS context when collected. A deterministic Lookup brief locally organizes the current task, normalized facts, contradictions, unknowns, source-quality counts, observed relationship types, and suggested manual actions; it does not add an analyst assertion or make a decision. The separate Lookup JSON evidence export may contain contact data published in raw RDAP or WHOIS responses and the bounded page-identity evidence displayed by Lookup. A selected current-schema Lookup evidence JSON file of up to 5 MB can be replayed locally in the browser. Replay validates schema, nesting and entry bounds, calculates the file SHA-256 digest, can compare it with an explicitly supplied trusted checksum, and displays bounded normalized facts and source states without uploading the file, contacting the target, rendering raw payloads, or treating import time as observation time. A reviewed case-response packet can contain analyst-entered incident facts, exact HTTP(S) URLs, UTC observation time, and separately attributed contact routes. The selected audience profile states included and excluded evidence, expected redactions and attachments, follow-up fields, and any missing evidence for registrar, registry, network/hosting, security-contact, browser/blocklist, or internal-SOC review. Its local preflight blocks incomplete required incident facts and exposes cautions for evidence selection, analyst reasoning, recipient provenance, disposition, freshness, contradictions, and action tracking. JSON, Markdown, and email-text forms include the bounded profile, preflight, and action-outcome summary, state that review is required, and state that no submission occurred. Defensive domain exports require an explicit reviewed selection, exclude configured official, allowlisted, and common-infrastructure domains, and include an expiry, provenance manifest, exclusions, and rollback instructions. Wildcard RPZ entries require a separate opt-in. WHOISleuth neither sends those files nor changes a defensive system. A deliberate unified workspace archive can contain cases and their analyst notes, evidence pins, decisions, response actions, campaigns, Brand Profiles, watchlists, shortlist entries, custom detection rules, retained relationship observations, compact saved Bulk sessions, website profile snapshots, investigation templates, active-profile selection, and theme preference. Workspace archive version 5 adds website-profile snapshots after version 4 added investigation templates; versions 1 through 4 remain readable without inventing missing later sections. The archive uses a versioned manifest with per-section SHA-256 checksums, previews conflicts before a non-destructive merge, and excludes login sessions, passwords, API credentials, hosted-monitor encryption keys, raw upstream payloads, tab state, Certificate Transparency history, and unrelated browser storage. The recommended download uses browser-native PBKDF2-HMAC-SHA-256 and AES-256-GCM to encrypt that ordinary archive with a passphrase. The passphrase and derived key are not persisted, sent, logged, or recoverable. A separately labelled unencrypted download remains available. Encrypted import decrypts in browser memory, reports the same error for a wrong passphrase or corrupted authenticated ciphertext, and then applies the ordinary archive validation and reviewed merge. Archive encryption cannot protect an unlocked Console from same-origin code, a malicious extension, device malware, a keylogger, or a weak or reused passphrase. Nothing is uploaded or retained by the server when you export or import.</p>
  <p><strong>Offline CLI verification and diagnostics.</strong> <code>verify-artifact</code> reads one local supported archive, packet, or signed review artifact and reports only its contract, integrity state, and bounded size or count metadata by default. <code>inspect-archive</code> first performs the same verification, then reports bounded section summaries. Exact search is limited to allowlisted target fields and returns a digest by default; values require explicit <code>--reveal</code>. It never searches notes, contacts, raw evidence, or arbitrary fields. An encrypted archive passphrase is accepted only through a separate bounded local file and is not printed or retained. <code>source-report</code> reads bounded local CLI Lookup or Bulk documents or merges its own target-free reports and reports only fixed source identifiers with aggregate states, durations, truncation, and rate-limit counts. It does not retain or output targets, queries, endpoints, source limitations, raw evidence, or provider payloads. Optional <code>sign-artifact</code> signs only a supported artifact that passes offline verification, using an externally managed Ed25519 key file. The signed package contains the original artifact, signature time, signature, and public key; WHOISleuth does not generate, store, recover, rotate, publish, or establish trust in signing keys. These commands make no network request and do not upload input.</p>

  <p><strong>Active guide context.</strong> The shared context tray derives the active profile, matching case, latest deliberately retained observation time, and next manual action from bounded browser-local records without copying them into guide storage. Its work-plan detail can be dismissed without clearing the context. Changing the target requires confirmation and creates a fresh guide for the same recipe rather than transferring progress between domains. Neither action starts collection or changes a case.</p>

  <h2 id="privacy-retention">7. Retention</h2>
  <p>Ordinary lookup and posture results are not written automatically to an investigation database. An analyst can explicitly retain only the bounded records described above. Bounded registry bootstrap and selected public registration responses can remain briefly in server memory to reduce duplicate upstream requests; optional security.txt and external intelligence results are not cached. Browser-local investigation data persists until you remove it or clear site data. Optional hosted scheduled-monitoring ciphertext persists until the operator removes its Blob; disabling the worker does not delete it. You are responsible for securing and deleting downloaded exports.</p>

  <h2 id="privacy-security">8. Security</h2>
  <p>Controls include a shared-password gate, signed HttpOnly cookies, per-IP rate limiting, a restrictive Content Security Policy, and SSRF/DNS-rebinding protections for outbound checks. No system is perfectly secure, and the service is provided as is.</p>

  <h2 id="privacy-rights">9. Your rights</h2>
  <p>Depending on your jurisdiction, you may have rights to access, correct, delete, restrict, or object to processing. The service has no individual user-account database. Requests concerning registry-published data should generally go to the responsible registry or registrar; saved lists and exports remain under your control, and an operator who enables hosted scheduled monitoring must also manage its encrypted Blob state.</p>

  <h2 id="privacy-transfers">10. International transfers</h2>
  <p>Registries and hosting infrastructure may operate in other countries, so requested lookups can involve international processing inherent to RDAP, WHOIS, DNS, and web hosting.</p>

  <h2 id="privacy-children">11. Children's privacy</h2>
  <p>The service is not directed at children and is not knowingly used to collect personal data from children.</p>

  <h2 id="privacy-changes">12. Changes</h2>
  <p>This policy may change over time. Material changes will update the date shown above.</p>

  <h2 id="privacy-contact">13. Contact</h2>
  <p>Public support requests are not handled through this site. People authorised to use the protected console should contact the operator who provided access; data-subject requests should use the contact designated by that operator. The repository contains the complete <a href="https://github.com/slicedearth/whoisleuth/blob/main/PRIVACY.md" target="_blank" rel="noopener">privacy documentation</a>.</p>
</article>
</div>

<style>
  .policy-layout{display:grid;grid-template-columns:190px minmax(0,860px);align-items:start;gap:14px}
  .policy-index{position:sticky;top:18px;display:grid;gap:3px;padding:12px}
  .policy-index p{margin:3px 8px 8px}
  .policy-index a{padding:7px 8px;border-radius:var(--radius-sm);color:var(--muted);font:650 var(--text-2xs) var(--mono)}
  .policy-index a:hover,.policy-index a:focus-visible{background:rgb(var(--accent-rgb) / .07);color:var(--accent)}
  .policy{min-width:0;padding:clamp(22px,4vw,42px)}
  .policy>p:first-child{margin-top:0;color:var(--muted)}
  .policy h2{margin:2rem 0 .65rem;font:700 1.05rem var(--mono);color:var(--accent2);scroll-margin-top:24px}
  .policy p,.policy li{color:var(--muted);line-height:1.7}
  .policy strong{color:var(--text)}
  .policy a{color:var(--accent);text-decoration:underline;text-underline-offset:3px}
  .policy code{padding:2px 5px;border:1px solid var(--border);border-radius:4px;background:var(--bg);color:var(--text)}
  .policy li+li{margin-top:.35rem}
  @media(max-width:820px){
    .policy-layout{display:block}
    .policy-index{position:relative;top:auto;grid-template-columns:repeat(2,minmax(0,1fr));gap:3px;margin-bottom:12px;overflow:visible}
    .policy-index p{grid-column:1/-1;margin:3px 8px 8px}
    .policy-index a{min-width:0;overflow-wrap:anywhere}
    .policy h2{scroll-margin-top:18px}
  }
</style>
