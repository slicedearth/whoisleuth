// Browser-safe bounds shared by candidate discovery responses and the
// Discover-to-Bulk handoff. These are provenance labels, not domain names;
// they remain bounded independently of the target-domain contract.

export const MAX_CANDIDATE_SOURCE_LENGTH = 253;
